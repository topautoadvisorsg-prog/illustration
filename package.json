{
  "name": "wildlands-publishing-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Automated book publishing pipeline for The Wildlands series.",
  "workspaces": [
    "backend",
    "shared",
    "frontend"
  ],
  "scripts": {
    "audit:layouts": "node scripts/audit-layout-library.mjs",
    "verify:pipeline": "node scripts/verify-pipeline.mjs",
    "dev:backend": "yarn workspace @wildlands/backend dev",
    "build:backend": "yarn workspace @wildlands/backend build",
    "typecheck": "yarn workspaces run typecheck",
    "predeploy": "yarn workspace @wildlands/shared build && yarn workspace @wildlands/shared typecheck && yarn workspace @wildlands/backend typecheck",
    "prepare": "git config core.hooksPath .githooks || exit 0",
    "lint": "yarn workspaces run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "yarn@1.22.22"
}
