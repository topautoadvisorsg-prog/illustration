# Dependencies
node_modules/
.pnp/
.pnp.js

# Build outputs
dist/
build/
*.tsbuildinfo

# Env files — NEVER commit real keys
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python supervisor placeholder artifacts
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Pipeline artifacts (per-project, never committed)
backend/storage/
spikes/output/
spike-output/

# Test artifacts
coverage/
.nyc_output/

# Sentry
.sentryclirc

# Misc
*.tgz
.cache/

repomix-output.xml

# Local validation/proof artifacts (Claudio review sessions)
ch1_pages/
ch1_val_pages/
compare/
raw_imgs/
showcase/
validation-artifacts/
BEFORE_AFTER_*.png
CHAPTER1_5PAGE_PROOF.pdf
ch1_proof_v2*.pdf
ch1_validation.pdf
prompt_audit/
# Render-test scratch (UI screenshots, blueprint PNGs, proof PDFs)
live_shots/
nav_shots/
rz_test/
step3/
verify/
vis_shots/
experimental_*.png
experimental_*.pdf
experimental_*.py
std_*.png
wpr_*.png
badge_preview.html
printprep_*.png

# Session-byproduct PNGs / forensic dumps from rendering & pagination diagnostics
batch2*.png
batch2v2*.png
render_*.png
forensic_prompt.txt
forensic_spec.json

# Geometry / utilization diagnostic outputs (regenerable via the scripts)
backend/geometry-audit.png
backend/geometry-audit.svg
backend/utilization-report.png

# Graphify (knowledge-graph) outputs — large, regenerable
graphify-out/

# Whole-book render outputs & baselines (paid renders + no-spend previews).
# Local QA references only — never committed. Regenerable (paid ones cost spend).
baseline/
test-book-review-renders/
test-book-review-renders-live/
backend/catalog/
backend/cover-render.png
backend/title-blueprint*.png
backend/title-prompt.txt
backend/titlepkg.json
complete-test-book-*.pdf

# Front-matter / chapter-run scratch reports (no-spend, regenerable)
fm-report*.json
fm-after-fix.json
live-*.json
pages-run.json
preview-ch01p001.json
railway-status-after-artifact.json
supervisor-*.json
ch01-*.txt
